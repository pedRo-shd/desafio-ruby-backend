<% flash.each do |type, msg| %>
  <div id="notice">
    <%= msg %>
  </div>
<% end %>

<h1>Stores</h1>

<table>
  <thead>
    <tr>
      <th scope="col">brand</th>
      <th scope="col">Owner name</th>
      <th scope="col">Financial Transactions</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @stores.each do |store| %>
      <tr>
        <td><%= store.brand %></td>
        <td><%= store.owner %></td>
        <td><%= link_to 'Ver operacões financeira desta loja', financial_transactions_path(store) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Import CNAB', financial_transactions_imports_new_path %>
