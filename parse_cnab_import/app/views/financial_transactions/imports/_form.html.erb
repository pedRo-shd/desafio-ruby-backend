<%= form_with(url: financial_transactions_imports_path, local: true, method: :post) do |f| %>

  <div class="field">
     <%= f.file_field :file %>
  </div>
  <hr>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
